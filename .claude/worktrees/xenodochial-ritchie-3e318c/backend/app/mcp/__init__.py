"""
MCP (Model Context Protocol) package.

This package provides a standalone MCP server that exposes agent tools
(appointment scheduling, etc.) as protocol-standard tools that any
LLM provider can consume.
"""

# Railway prompt base

Fuente: Railway, proyecto `captame`, entorno `production`, servicio `inmo`.
Broker: `Demo Inmobiliaria` (ID 1).
Snapshot activo: `prompt_versions.id=10`, versión `auto-010`.
Extracción: 31 de julio de 2026.

Estos archivos son una copia local de los prompts almacenados en producción. El prompt activo contiene la configuración efectiva; los prompts de agente corresponden a las claves `_agent_qualifier` y `_agent_scheduler` de `broker_prompt_configs.situation_handlers`.

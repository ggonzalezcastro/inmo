[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "celery -A app.celery_app worker --loglevel=warning --concurrency=2"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

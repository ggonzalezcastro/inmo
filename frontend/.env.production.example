# Production Environment Variables for Frontend

# URL pública del backend. Vite la incorpora durante el build de la imagen.
# Se mantienen ambas variables mientras conviven clientes JS y TypeScript.
VITE_API_URL=https://api.captame.cl
VITE_API_BASE_URL=https://api.captame.cl

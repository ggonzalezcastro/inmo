{
  "$schema": "https://railway.app/railway.schema.json",
  "services": {
    "backend": {
      "build": {
        "builder": "dockerfile",
        "dockerfile": "backend/Dockerfile"
      },
      "deploy": {
        "startCommand": "alembic upgrade head && python scripts/seed_brokers.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT --log-level error",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 3
      }
    },
    "celery-worker": {
      "build": {
        "builder": "dockerfile",
        "dockerfile": "backend/Dockerfile.celery-worker"
      },
      "deploy": {
        "startCommand": "celery -A app.celery_app worker --loglevel=error --concurrency=4",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 3
      }
    },
    "celery-beat": {
      "build": {
        "builder": "dockerfile",
        "dockerfile": "backend/Dockerfile.celery-beat"
      },
      "deploy": {
        "startCommand": "celery -A app.celery_app beat --loglevel=error",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 3
      }
    },
    "frontend": {
      "build": {
        "builder": "nixpacks"
      },
      "deploy": {
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 3
      }
    }
  }
}
